# MuseTalk V1.5 inference config for Avatar Pipeline
# Paths are relative to the ~/MuseTalk directory at runtime.
# This file lives in the pipeline repo — setup.sh symlinks/copies it if needed.

video_path: ""           # set at runtime via CLI args in musetalk_infer.py
audio_path: ""           # set at runtime via CLI args in musetalk_infer.py
bbox_shift: 0
fps: 25
output_dir: ""           # set at runtime via CLI args in musetalk_infer.py
use_float16: true
